return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      max_name_length = 100,           -- Set the maximum number of characters for a buffer name
      max_width = 100,                 -- Set the maximum width of a tab
      show_buffer_close_icons = false, -- Remove close icons to save space
      separator_style = "thin",        -- Use a thin separator for a cleaner look
    },
  },
}
